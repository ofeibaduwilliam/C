#include <stdio.h>

int main(){
    char characterName[] = "John";
    int characterAge = 35;

    printf("There once as a man named %s\n", characterName);
    printf("He was %i years old\n", characterAge);
    printf("He really liked the name %s\n", characterName);
    printf("But he hated being %i\n", characterAge);
    return 0;
}
